package com.hmg.role.util.enums;

/**
 * Identifies if an operand is an attribute or a literal. The term {@link OperandType} might not
 * mathematically correct, I don't know, never took that abstract algebra class. If you had better
 * one please let me know.
 */
public enum OperandType {
    ATTRIBUTE,
    LITERAL;
}
